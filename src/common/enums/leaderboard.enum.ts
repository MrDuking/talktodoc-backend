export enum Rank {
    BRONZE = "bronze",
    SILVER = "silver",
    GOLD = "gold",
    MASTER = "master",
    EPIC = "epic",
    LEGEND = "legend"
}

export enum LeaderboardType {
    DAILY = "daily",
    WEEKLY = "weekly",
    MONTHLY = "monthly",
    ALLTIME = "alltime"
}
